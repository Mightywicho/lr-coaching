# Bitácora — LR Coaching

> Estado vivo del proyecto. Leer al iniciar cada sesión; actualizar al cerrar cada bloque.
> La arquitectura y el workflow de cambios/deploy viven en el skill `lr-coaching-context` (no duplicar aquí).
> Última actualización: 2026-07-14 (18ª sesión: **Confirmar contexto del atleta al aplicar cualquier plantilla — DESPLEGADO en prod** (commit "Confirmar contexto (bloque/experiencia/fase/inicio) al aplicar plantilla + validación de fecha", 5350→**5374 líneas** (5376 en GitHub, diferencia de 2 = conteo de líneas del visor, contenido idéntico — subido con `file_upload` directo desde disco, no por bash), Pages run #48 OK, app carga como coach sin errores de consola con cache-bust, sesión de Luis ya autenticada en el navegador). Al hacer clic en «Aplicar»/«Asignar» sobre CUALQUIER plantilla (rutina o nutrición, no solo IA) ahora se abre un modal previo que pide **bloque, experiencia, fase actual y —solo rutinas— fecha de inicio de ese bloque** (antes fija a "hoy" sin poder cambiarla); los valores quedan guardados en el perfil del atleta (mismos campos que la barra de Contexto) y son los que alimenta el ajuste automático de las plantillas IA. Incluye validación: fecha de inicio no puede ser futura (`max` en el input + chequeo JS) ni anterior al fin del último bloque archivado en `routine.history` (toast bloqueante, no cierra el modal). **Falta prueba funcional end-to-end en producción** (Luis no tiene plantillas guardadas todavía en la cuenta real — no se creó una de prueba para no tocar datos de sus atletas reales; Luis puede probarlo la próxima vez que guarde/asigne una plantilla).)
> Sesión previa (17ª): **Ajuste automático al asignar plantillas del importador IA — DESPLEGADO en prod** (commit "Ajuste automático al asignar plantillas IA: rutina (prioridades/volumen) y nutrición (macros)", 5247→**5350 líneas** (5351 en GitHub, diferencia de 1 = archivo sin salto de línea final; contenido idéntico), Pages run #46 OK, app carga como coach sin errores de consola con cache-bust). Falta prueba end-to-end con un archivo real (importar → guardar como plantilla IA → asignar a atleta → confirmar en pantalla el ajuste) — no se corrió para no gastar la Edge Function sin necesidad; la lógica sí se validó con node --check y con una prueba de regresión en vivo sobre "Usar recomendados" en el atleta real "Luis" (mismo resultado que antes de refactorizar, valor de prueba repuesto a su original).
> Sesión previa (16ª): **Matching fuzzy en importador IA + Asignar receta al plan — DESPLEGADO en prod** (commit "Importador IA: matching fuzzy…", 5227→**5247 líneas**, GitHub=local verificado, app carga como coach sin errores de consola con cache-bust). Faltaba confirmación visual funcional del botón «Asignar receta» (Luis puede verificarlo: abrir atleta → Nutrición → modo «Menú cerrado» → botón «Asignar receta»).
> Sesión previa (15ª): **Importador universal con IA DESPLEGADO y VERIFICADO en prod** (`30a0105`, 5227 líneas). Secret `ANTHROPIC_API_KEY` puesto por Luis; function `parse-plan` probada end-to-end (200, ~4.3s, `ok:true`); index.html desplegado (GitHub=local=5227, Pages OK, consola sin errores); probado con archivo real → rutina 3 días/10 ej. + nutrición 2400 kcal + suplementos.
> Sesión previa (14ª): Recetario v1 — tabla `recipes` + importador + vista navegable/filtrable. DESPLEGADO y VERIFICADO en prod (`a00d693`, 5137 líneas): 240 recetas importadas, tarjetas + detalle OK. Asignar-receta-al-plan sigue pendiente.

## Estado actual
- `index.html` en `main`: **5374 líneas**. **Producción al día y VERIFICADA** (carga sin errores de consola, sesión de coach ya autenticada). Pendiente probar en vivo: (1) el flujo IA completo con un archivo real, (2) el nuevo modal de contexto al aplicar una plantilla (Luis no tiene plantillas guardadas en la cuenta real todavía).
- **Confirmar contexto al aplicar plantilla (sesión 18, NUEVO):** `tpl-apply-ask` ya no aplica directo — abre un modal con bloque/experiencia/fase actual (siempre) y fecha de inicio del bloque (solo rutinas, prellenada a hoy, `max` = hoy). Al confirmar (`tpl-apply-do`) esos valores se escriben en `profile.block/experience/phase` (mismos campos que la barra de Contexto del atleta) antes de aplicar la plantilla, y si es rutina también sobreescriben `routine.startDate` (antes fijo a `todayISO()` sin poder cambiarse). Validación al confirmar: fecha futura → bloquea con toast; fecha anterior al `endDate` del último bloque en `routine.history` → bloquea con toast (mensaje incluye ambas fechas). Aplica a cualquier plantilla (rutina o nutrición, manual o IA) porque bloque/experiencia/fase también alimentan `nutriTargets` (cálculo de macros) además del ajuste de rutina.
- **Ajuste automático de plantillas IA (sesión 17, NUEVO):** al asignar una plantilla a un atleta (`tpl-apply-do`), si la plantilla tiene `source:'ai-import'` (flag agregado solo a las guardadas desde el importador IA, no a las manuales/CSV) se dispara un ajuste automático:
  - **Rutina** (`applyAiRoutineAdjust`): conserva los días/ejercicios del documento tal cual (decisión de Luis: no regenerar con autoSplit). Suma las series ya programadas por grupo muscular (`programmedVolumeByGroup`, requiere que los ejercicios estén vinculados a la biblioteca por `libId`) y de ahí **infiere la prioridad 0-4 por grupo** comparándola contra sus landmarks (`priorityFromVolume`: sin series→0, ≥VMA→4, ≥VME→3, ≥VM→2, si no →1). Con esas prioridades nuevas, rellena objetivos semanales (`autoFillVolTargets`, antes inline en el botón "Usar recomendados") y reparte esos objetivos entre las sesiones ya existentes (`autoDistributeDayPlan`, antes inline en "Repartir automático"). Ambos botones de la UI ahora llaman a estas mismas funciones compartidas (refactor sin cambio de comportamiento, verificado en vivo).
  - **Nutrición** (`applyAiNutriAdjust`): los kcal/macros del documento **siempre se descartan y se recalculan** con el perfil real del atleta (decisión de Luis) vía `nutriTargets` (mismo cálculo de TDEE/Ten-Haaf/PAL/déficit-superávit que ya usaba `autoNutrition`, extraído a función compartida). La estructura de comidas/alimentos del documento se conserva; `rescalePlanMacros` reescala cada macro (P/C/G) del documento con su propia razón (nuevo objetivo ÷ total viejo de esa macro) para no distorsionar la proporción entre alimentos de una misma comida. Si al atleta le falta peso o altura, no se puede calcular el objetivo real → se avisa en el toast y se dejan los valores del documento tal cual.
  - **Momento del ajuste**: al asignar la plantilla a un atleta (no al importar el archivo), porque ahí sí existen datos reales del atleta (peso, %grasa, experiencia, fase) para calcular algo específico y no genérico — coincide con el filtro del proyecto de "simplificar sin volverse mediocre o general".
  - Badge visual "IA" junto al nombre de la plantilla (vista Plantillas y modal de aplicar) para que el coach sepa cuáles disparan el ajuste automático al asignarlas.
  - **Gotcha:** si un ejercicio importado por IA no quedó vinculado a la biblioteca (`libId` nulo — pasa cuando el coach no hizo clic en "Enlazar" durante el preview), ese ejercicio no cuenta en `programmedVolumeByGroup` y su grupo puede quedar con prioridad 0 aunque sí tenga series programadas. Conviene vincular todo antes de guardar como plantilla.
- `index.html` en `main` (histórico, commit `30a0105`, 5227 líneas). **Producción al día y VERIFICADA** (carga como coach sin errores de consola; Importador IA funcionando end-to-end; Recetario con 240 recetas navegable; import de biblioteca funciona; videos de YouTube embeben; Pages sirviendo la versión nueva). `recetario-base.json` (240 recetas) también en el repo.
- **Biblioteca de ejercicios importada**: 55 → **214 ejercicios** (159 de Luis + seed). Cada uno con su video de YouTube. Grupos mapeados desde sus códigos (QUADS→Cuádriceps, DA→Deltoide anterior, etc.). Falta (opcional): asignar categoría I–IV y equipo real a los importados (entraron con cat vacía y equipo «Otro»).
- **Feature "check-in estructurado + reporte visual" COMPLETA y verificada funcionalmente** (Luis confirmó las 3 tareas de `a37bc30`).
- Auditoría multi-coach (`Auditoria-Multicoach.md`) cerrada al 100% (C1–C5 + hardening, verificado con cuentas reales e impersonación SQL).
- Toda la data compartida coach⇄atleta vive en tablas con RLS; el blob `app_state` solo lleva perfil del coach, librería, config y plantillas (NO logs, chat, rutinas, planes ni perfiles de atleta). El atleta lee el blob del coach (SELECT) → sirve para difundir config global como el color de marca.
- `Ideas.md` (misma carpeta): backlog de la fase funcional.
- **Recetario v1 (DESPLEGADO y VERIFICADO, `a00d693`).** Tabla `recipes` creada y migrada (coach_uid uuid + rid text + data jsonb, RLS solo-coach para las 4 ops, realtime, unique(coach_uid,rid); advisors sin hallazgos nuevos). Verificado en prod: importadas las 240 recetas al catálogo real de Luis (DB confirma 240 filas), grid + detalle (ingredientes/preparación/macros) OK, sin errores de consola. Gotcha UI observado: en el modal de importar, un clic fuera del botón cae en el backdrop y cierra el modal sin guardar; el `onConflict(coach_uid,rid)` evita duplicados si se reintenta. En `index.html`: módulo Recetario — `hydrateRecipes` (coach-only, enganchado en `initAfterAuth` y `refreshAllData`), importador (botón "Importar recetario base" que hace `fetch('recetario-base.json')` + "Importar JSON" por archivo; dedup por `id`; preview por categoría), vista `recetario` en el nav del coach (ícono `I.recipe`), con búsqueda (nombre/ingrediente/etiqueta) y filtros categoría/etiqueta/kcal, y modal de detalle (ingredientes + preparación + macros). `recetario-base.json` (240 recetas, 116KB) ya está en la carpeta del repo, falta subirlo. Parser del PDF verificado: 240 recetas, categorías exactas 60/90/60/30, sin duplicados; dos formatos de macros en el PDF (R001–R080 «Macros estimados P/HC/G» + kcal aparte; R081–R240 «Macros aprox: kcal | P | HC | G»), ambos parseados; solo R014 sin kcal (calculado desde macros). Decisión técnica clave para asignar-al-plan: los ingredientes NO traen macros por ingrediente → la receta entrará como **comida con `m.p/m.c/m.f` = totales de la receta** (para que el marcado reste exacto) y los ingredientes/preparación viajarán en un campo `m.recipe` que se muestra completo; NO meter ingredientes como items con macros 0 (rompería `mealMacros`).

## Hecho (resumen; detalle en git)
- **Confirmar contexto al aplicar plantilla + validación de fecha** (5350→5374, sesión 18). Ver detalle completo arriba en "Estado actual". Resumen: modal previo en `tpl-apply-ask` pide bloque/experiencia/fase (+fecha de inicio en rutinas) antes de aplicar cualquier plantilla; `tpl-apply-do` guarda esos valores en el perfil del atleta y valida la fecha (no futura, no anterior al último bloque archivado). Pendiente: prueba funcional en producción con una plantilla real (Luis no tiene ninguna guardada en la cuenta real todavía).
- **Ajuste automático al asignar plantillas del importador IA** (5247→5350, sesión 17). Ver detalle completo arriba en "Estado actual". Resumen: `tpl-apply-do` detecta `t.source==='ai-import'` y llama `applyAiRoutineAdjust`/`applyAiNutriAdjust`; nuevas funciones `programmedVolumeByGroup`, `priorityFromVolume`, `autoFillVolTargets`, `autoDistributeDayPlan`, `nutriTargets`, `rescalePlanMacros`; refactor de `autoNutrition` (ahora usa `nutriTargets`), de "Usar recomendados" (`voltargets-auto`) y "Repartir automático" (`plan-auto`) para compartir la misma lógica sin cambiar su comportamiento (regresión probada en vivo). Pendiente: prueba end-to-end con archivo real (import IA → guardar plantilla → asignar → ver ajuste en pantalla).
- **Matching fuzzy en importador IA + Asignar receta al plan** (5227→5247, sesión 16). (1) `aiPreviewRender()` ahora usa `libBestMatch` (mismo patrón que el import CSV): los ejercicios sin `libId` exacto muestran botón «Enlazar con «X»» (`data-act="aiimport-link"`) + contador de sugeridos + notebox; el handler `aiimport-link` preserva los nombres editados (`ai_rt_name`/`ai_nt_name`) al re-render. (2) **Asignar receta al plan**: botón «Asignar receta» en la tarjeta Menú del coach (solo modo `menu`) → modal con buscador en vivo (`data-live="recpick-q"`, filtra `#recpick_list` sin cerrar el modal) → `pmeal-recipe-add` mete la receta como **comida** con `m.p/m.c/m.f` = totales (mapea `macros.g`→`f`) + `m.recipe={rid,kcal,time,ingredients,steps}`. Render de comida-receta (helper `mealRecipeBox`) en el editor del coach y en la vista Nutrición del atleta (badge «receta» + ingredientes + preparación). `planMenuTotals` corregido para sumar comidas sin items (antes solo sumaba items → la receta no contaba). Marcado en «Hoy» ya usaba `mealMacros` → resta los macros exactos de la receta. **Gotcha/límite de diseño:** la asignación vive solo en modo Menú (es donde hay menú de comidas); en modo Macros el atleta no ve menú, así que el botón no aparece ahí a propósito.
- **Importador universal con IA — rutina/nutrición desde archivo** (`30a0105`, 5137→5227). Vista Plantillas → botón **«Importar archivo (IA)»** (`aiimport-pick`). `extractText()` lee CSV/TXT directo, XLSX (SheetJS), PDF (PDF.js), DOCX (mammoth), todo lazy por cdnjs. Llama la Edge Function `parse-plan` (Supabase `vflzzokyaiddsxyeoigu`, v1, verify_jwt=true, modelo `claude-haiku-4-5-20251001`, `tool_choice` forzado a `emit_plan`, secret `ANTHROPIC_API_KEY`); devuelve `{ok,result:{found,routine,nutrition}}`. `aiRoutineDays()` mapea la rutina auto-vinculando a biblioteca por `libByName`; `aiNutriTpl()` arma la plantilla de nutrición (planes+comidas+items+suplementos). Preview editable (nombre) + «Guardar como plantilla». Verificado en prod: function 200/~4.3s, líneas GitHub=local=5227, Pages OK sin errores de consola, archivo real → detecta rutina 3 días/10 ej. (5 vinculados) + nutrición 2400 kcal + suplementos. Fuente de la function en `outputs/parse-plan.ts`. **Gotcha de matching:** el AI import usa solo `libByName` (clave exacta), NO el fuzzy `libBestMatch` que sí ofrece el import de rutina por CSV → ejercicios con nombre distinto ("Prensa" vs "Prensa de pierna") quedan sin vincular (ver backlog).
- **Emparejado por clave normalizada + importador CSV de biblioteca + confirmación asistida** (`74851b9`, 4941→5017). (1) Nuevo helper `exKey(s)` = `normTxt` (sin acentos, minúsculas) + colapsar espacios. Se usa en los 4 puntos de emparejado ejercicio⇄biblioteca (`libByName`, `linkRoutinesToLib`, dedup del seed, `keyIdent`): resuelve de raíz que rutinas con tilde/mala escritura no enlazaran (ej. "Séntadilla"≡"SENTADILLA"). (2) Botón **Importar CSV** en la vista Biblioteca: parsea `Ejercicio,MÚSCULO/OBJETIVO,Link`, mapea códigos vía `LIB_MUSCLE_MAP`, dedup por `exKey`, guarda el link como video YouTube, preview agrupado por músculo antes de confirmar. Filas sin nombre o sin grupo muscular se ignoran (los 4 no-musculares de Luis venían vacíos). (3) En **importar rutina**: si un ejercicio no calza exacto, `libBestMatch` (similitud Dice por bigramas, umbral 0.55) sugiere el más parecido de la biblioteca para enlazar con un clic (`rtimport-link`) — cubre typos que no son de acento. Verificado en prod: import real de 159 ejercicios (55→214), videos embeben, sin errores de consola.
- **Tema de marca configurable** (`0b68462`, 4868→4941). Un solo color de acento re-tematiza toda la app vía variables CSS (`--volt` + variantes de transparencia; peligro en `--red` + variantes). Motor `applyBrand(hex)`: sube brillo en tema oscuro para legibilidad, calcula texto por contraste, y si el acento es rojo pasa el color de **peligro/eliminar a ámbar** (si no, coral). `ensureBrand(S)` default `#c80508`. Se guarda en `S.brandColor` (blob, coach-only escribe) → **los atletas lo heredan automático** y se propaga en vivo (aplicado en `initAfterAuth` y `refreshAllData`). Nueva vista **Configuración** en el nav del coach (ícono engranaje): presets (rojo/lima/azul/esmeralda/violeta/naranja) + HEX libre + vista previa + link de videollamada. Acciones `brand-set`/`brand-reset`. Verificado en prod: cambio en vivo rojo↔azul, peligro ámbar↔coral, sin errores. Paleta de marca de Luis: negro `#000`, blanco `#F6F6F6`, rojo `#C80508`.
- **Reporte de fuerza + rediseño check-in + import rutina CSV** (`a37bc30`, 4744→4868). (1) Sección "Rendimiento en fuerza" en el reporte con ejercicios clave (1RM Epley); (2) check-in vista coach con barras por señal + Δ vs semana previa; (3) importar rutina desde CSV como plantilla. Verificado funcionalmente por Luis.
- **Phase 3 — Reporte visual** (`821949e`): pestaña Reporte por atleta (peso, medidas/%grasa/WHR, adherencia/bienestar), ventana 4/8/12 sem, export PNG (html2canvas) + PDF (print).
- **Phase 2 — Medidas corporales** (`5cd051f`): tabla `measurement_logs` (RLS+realtime), 5 medidas capturadas por el atleta, %grasa Navy + WHR en Historial del coach.
- **Hambre en el check-in** (`cbd2032`): señal de nutrición, no suma a la autorregulación de carga.
- **Plantillas de rutinas y nutrición** (`31cc44e`): `S.templates` en el blob; guardar/asignar/aplicar conservando historial.
- **Semáforo de adherencia** (`a2406ea`): verde/amarillo/rojo por días sin registrar + fatiga.
- **Quick wins** (`f2bc95a`): fix acordeones al re-render, link de revisión por ejercicio, botón Calendly.
- **Backend/RLS**: `athlete_plans`, `athlete_profiles` (shared/private/diet), 6 series de logs + `chat_logs` con RLS+realtime, `athlete_access`/`athlete_invites` con RPCs SECURITY DEFINER, invitaciones a 7 días, purga completa al borrar atleta, password mín. 8 con letras+números.

## Backlog priorizado (ideas, no urgentes)
- **Probar el flujo de ajuste automático IA con un archivo real** (pendiente de sesión 17): importar → guardar como plantilla → asignar a un atleta con perfil completo (peso/altura/%grasa/experiencia/fase) → confirmar en pantalla que prioridades/objetivos/distribución (rutina) y kcal/macros (nutrición) quedan bien.
- **Importador IA — asignar la plantilla importada directo a un atleta** desde el mismo preview (hoy: guardar como plantilla → ir a Plantillas → «Asignar a atleta»). Atajo opcional.
- **Receta al plan en modo Macros (opcional).** Hoy «Asignar receta» solo aparece en modo Menú. Si Luis quiere asignar una receta a un plan en modo Macros, habría que decidir cómo se la muestra el atleta (el modo Macros no renderiza menú). Posible: mostrar un bloque «Recetas sugeridas» aparte de las barras de macros.
- **Editar la receta ya asignada al plan (opcional).** Hoy una comida-receta solo se puede quitar (`pmeal-del`) y volver a asignar. Falta un editor rápido para ajustar hora/porción/macros sin re-buscar la receta. Cuidado: si se edita la porción, los macros `m.p/m.c/m.f` deberían reescalarse desde los totales de la receta para que el marcado en «Hoy» siga restando lo correcto.
- **Alerta automática en el check-in** cuando una señal cae ≥2 puntos vs la semana previa (o el Global baja de golpe) — para detectar lo importante sin leer el desglose. Aprobado por Luis.
- **Reporte comparado por bloque de entrenamiento** (usar `routine.history` con startDate/endDate/block) en vez de la ventana fija 4/8/12 sem.
- Reporte: versión de solo lectura para el atleta / generación mensual; elegir secciones por atleta.
- Plantillas: al crear atleta nuevo (selector en el modal), duplicar, días sueltos.
- Semáforo: botón "recordar" con mensaje prellenado; umbrales ajustables.
- Tema — gráficas: hacer que la métrica principal de las gráficas (hoy paleta categórica fija con `#cdf563` hardcodeado en `svgLine`/macros) siga el color de marca. Pasada aparte, con cuidado por el export a PNG (html2canvas resuelve `var()`, pero los SVG usan atributos `stroke`/`fill` que NO leen var() → habría que pasarlos a `style=`).
- Tema — fondo/tipografía: hoy el selector solo cambia el acento; el fondo sigue siendo el gris-azulado oscuro (`--bg #0b0d11`). Ofrecer en Configuración una opción de **fondo negro puro** (`#000`) para un look más "Physique Lab" (negro/blanco/rojo). Alcance: variable de fondo conmutable, guardada junto a `brandColor` en el blob para que los atletas también lo hereden.

## Gotchas vigentes
- **No confiar en bash para `index.html`** (el mount se trunca; `wc`/`node --check` dan números falsos). Usar Read/Edit y verificar líneas en GitHub tras subir. Para validar sintaxis de código nuevo: extraerlo a un `.js` en `outputs` (mount fresco) y `node --check` ahí.
- **Deploy** solo vía página de upload de GitHub (Claude in Chrome: `github.com/Mightywicho/lr-coaching/upload/main`) o push local de Luis (con `git pull origin main` antes). La subida usa el archivo real del disco, no el mount.
- Al escribir el commit en GitHub: al adjuntar el archivo la página se re-acomoda (~60px); verificar con screenshot que el texto entró en el campo *summary* antes de confirmar.
- En la app, los clics por `ref` de find a veces no disparan la delegación `data-act`; usar clics por coordenadas.
- Pages zombie/fallido: editar `.pages-kick` con la fecha y commitear (nuevo commit → nuevo run que sí despliega). Verificar SIEMPRE lo servido con cache-bust (`/lr-coaching/?v=…`).
- supabase-js: `.delete()/.update()` sin `await`/`.then` no se ejecuta.
- La identidad del atleta depende de `hydrateProfiles`; si falla, `initAfterAuth` recompone con `user_metadata`.

## Riesgo latente (vigilar, no accionar)
- Runs zombi de Pages #28/#31 quedaron "Queued". Si GitHub procesara el #28 re-desplegaría una versión vieja. Remedio: editar `.pages-kick` y commitear para forzar redeploy de `main`.

## Cuentas (identificadores, no contraseñas)
- Coach real: luis.ramirez.nutricion@gmail.com (`94ceb2ae-…`). Atletas: luigui_09@live.com.mx → `cqsurav`; aguillonpadillab@gmail.com → `hmfpv19`.
- Coach de prueba (aislamiento): mightywicho99@gmail.com (`dfd7a495-…`), sin atletas reales.
